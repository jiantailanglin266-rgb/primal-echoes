/**
 * シミュレーション（固定タイムステップ）の設定。
 * ヒット判定や無敵時間はフレームレートに依存させたくないため、
 * 描画とは独立した固定周期で core を更新する。
 */
export const SIMULATION_CONFIG = {
  /** 1 シミュレーションステップの長さ（秒）。60Hz。 */
  fixedDeltaSeconds: 1 / 60,
  /**
   * 1 描画フレームで実行を許す最大ステップ数。
   * タブ非表示などで大きな遅延が発生したとき、追いつこうとして
   * 「死のスパイラル」に陥らないよう上限を設ける。
   */
  maxSubStepsPerFrame: 5,
  /** 1 フレームで受け付ける実時間の上限（秒）。これを超えた分は捨てる。 */
  maxFrameDeltaSeconds: 0.25,
} as const;
